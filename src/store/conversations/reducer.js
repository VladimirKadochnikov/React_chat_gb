const initialState = {
  conversations: ["chat 1", "chat 2", "chat 3"],
};

export const conversationReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
